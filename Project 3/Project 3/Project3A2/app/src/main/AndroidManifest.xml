<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project3a2">
    <uses-permission android:name="edu.uic.cs478.s19.kaboom." />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Application A2"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project3A2">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".ReceiverClass" >
            <intent-filter>
                <action android:name="edu.uic.cs478.s19.kaboom." >
                </action>
            </intent-filter>
        </receiver>
    </application>
    <queries>
        <package android:name="com.example.project3a3" />
    </queries>
</manifest>